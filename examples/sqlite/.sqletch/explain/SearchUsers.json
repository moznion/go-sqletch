{
  "name": "SearchUsers",
  "guards": [
    "bit 0: status",
    "bit 1: email_prefix"
  ],
  "chooses": [
    "sort: email_asc, (default)"
  ],
  "shape_count": "8",
  "params": [
    "status: text (optional)",
    "email_prefix: text (optional)",
    "limit: integer"
  ],
  "columns": [
    "id: integer (not null)",
    "email: text (not null)",
    "status: text (not null)",
    "nickname: text (nullable)"
  ],
  "maximal_sql": "\n-- @param status: text\n-- @param email_prefix: text\n-- @param limit: integer\nSELECT u.id, u.email, u.status, u.nickname\nFROM users AS u\nWHERE TRUE\n\n\nAND (u.status = ?)\n\n\nAND (u.email LIKE ? || '%')\n\n\nORDER BY u.email ASC\n\nLIMIT ?;\n\n"
}

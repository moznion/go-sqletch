{
  "name": "UsersInStatuses",
  "guards": null,
  "shape_count": "2",
  "params": [
    "tenant_id: integer",
    "statuses: text",
    "limit: integer"
  ],
  "columns": [
    "id: integer (not null)",
    "email: text (not null)",
    "status: text (not null)"
  ],
  "maximal_sql": "\n-- @param tenant_id: integer\n-- @param statuses: text\n-- @param limit: integer\nSELECT u.id, u.email, u.status\nFROM users AS u\nWHERE u.tenant_id = ?\n  AND u.status IN (?)\nORDER BY u.id\nLIMIT ?;\n\n"
}

version: 1
dialect: mysql
server_version: "8.4"
database:
  # dsn: user@tcp(h)/db      # optional, literal only (no ${VAR}); empty = disposable testcontainers MySQL
schema:
  files: [db/schema.sql]
queries: [queries/*.sql]
output:
  package: gen
  path: gen

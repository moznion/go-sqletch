{
  "name": "ListAuditLogs",
  "guards": [
    "bit 0: after_id"
  ],
  "shape_count": "2",
  "params": [
    "tenant_id: int8",
    "after_id: int8 (optional)",
    "limit: int8"
  ],
  "columns": [
    "id: int8 (not null)",
    "actor_id: int8 (nullable)",
    "action: text (not null)",
    "created_at: timestamptz (not null)"
  ],
  "maximal_sql": "\nSELECT a.id, a.actor_id, a.action, a.created_at\nFROM audit_logs AS a\nWHERE a.tenant_id = $1\n\n\nAND (a.id \u003c $2)\n\nORDER BY a.id DESC\nLIMIT $3;\n"
}

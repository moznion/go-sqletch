version: "2"

linters:
  default: standard
  settings:
    staticcheck:
      checks: ["all"]

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/moznion/sqletch

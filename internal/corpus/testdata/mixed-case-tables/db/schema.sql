CREATE TABLE `apple` (
    id   BIGINT PRIMARY KEY,
    name VARCHAR(64) NOT NULL
);

CREATE TABLE `Zebra` (
    id      BIGINT PRIMARY KEY,
    stripes INT NOT NULL
);

CREATE TABLE `Mango` (
    id BIGINT PRIMARY KEY
);
